
using System;
using System.Dynamic;

static class Program
{
    static void Main()
    {
        // dynamic person = new ExpandoObjec();
        // person.name = "Budi";
        // person.age = 20;

        // Console.WriteLine(person.name + " " + person.age);

        object nama = "udin";
        string name = (string)nama;
    }
}